# ===========================================
# Server Configuration
# ===========================================
NODE_ENV=development
PORT=5000

# ===========================================
# MongoDB Atlas Connection
# ===========================================
# Connection string from MongoDB Atlas dashboard
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/placement-portal?retryWrites=true&w=majority

# ===========================================
# JWT Configuration
# ===========================================
# Secret key for signing JWT tokens (use a strong random string in production)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
# Token expiration duration
JWT_EXPIRES_IN=7d

# ===========================================
# SMTP Email Configuration (for OTP delivery)
# ===========================================
# Email address used to send OTP emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_EMAIL=your-email@gmail.com
# App password (NOT your regular Gmail password)
# Generate at: https://myaccount.google.com/apppasswords
SMTP_PASS=your-app-password

# ===========================================
# Client Configuration
# ===========================================
# Frontend URL for CORS whitelist
CLIENT_URL=http://localhost:3000

# ===========================================
# Rate Limiting
# ===========================================
# Max requests per window for general API
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100
# Max OTP requests per window
OTP_RATE_LIMIT_MAX=5
